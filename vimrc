

set encoding=utf-8		" Default to UTF-8
set fileencodings=utf-8		" Always.

set autoindent			" Copy indent from current line when starting a new line
				" (typing <CR> in Insert mode or when using the "o" or "O"
				" command).

"set cindent			" Indent according to the c standard indentation style.

set backspace=2			" Backspace over indents and newlines
				" Influences the working of <BS>, <Del>, CTRL-W
				" and CTRL-U in Insert mode. This is a list of items,
				" separated by commas. Each item allows a way to backspace
				" over something.

set nocompatible		" Turn off vi compatibility.

set noexpandtab			" Do not expand tabs to spaces

set number			" Show line numbers.

"set relativenumber		" Show relative line numbers.

set hlsearch			" When there is a previous search pattern, highlight all
				" its matches.

set incsearch			" Incremental Search
				" Start searching as soon as character entered.

set linebreak			" wrap at 'breakat' instead of last char
				" Do not break words when wrapping lines.

set ruler			" Show the line and column number of the cursor position,
				" separated by a comma.

"set showbreak=…		" prefix wrapped lines with ellipsis

set showcmd			" Show (partial) command in status line.


